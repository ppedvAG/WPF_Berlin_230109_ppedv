<Window x:Class="MVVM.View.StartView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:MVVM.ViewModel"
        xmlns:local="clr-namespace:MVVM.View"
        mc:Ignorable="d"
        Title="StartView" Height="167" Width="426"
        x:Name="Wnd_StartView">
    <Window.DataContext>
        <vm:StartViewModel/>
    </Window.DataContext>
    
    <StackPanel Margin="10">
        <TextBlock Text="Personendatenbank" FontSize="30" HorizontalAlignment="Center"/>

        <WrapPanel Margin="10" HorizontalAlignment="Center">
            <Button Command="{Binding LadeDbCmd}" Content="Lade DB"/>
            <Button Command="{Binding ÖffneDbCmd}" CommandParameter="{Binding ElementName=Wnd_StartView}" Content="Öffne DB"/>
        </WrapPanel>

        <TextBlock Text="{Binding AnzahlPersonen, StringFormat='Es wurden {0} Personen geladen.'}" HorizontalAlignment="Center"/>
    </StackPanel>
</Window>
